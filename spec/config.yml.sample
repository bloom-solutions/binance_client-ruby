---
host: "https://api.binance.com"

# Generic API keys, most likely the master key
api_key: "your key"
api_secret: "your secret"

# To test specific enpoints with sub account credentials
# When recording this must be an account with a balance history
sub_account_api_key: sub_account_api_key
sub_account_api_secret: sub_account_api_secret
sub_account_email: "some@subaccount.com"

# When recording the proxy spec
proxy: http://myproxy
